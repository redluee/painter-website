ADMIN_EMAIL=admin@sebastiaanpeters.nl
ADMIN_PASSWORD_HASH=<bcrypt hash, e.g. $2b$10$...>
JWT_SECRET=<random secret, at least 32 chars>

# Contact form email (Nodemailer via SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=info@sebastiaanpeters.nl
SMTP_PASS=<smtp password>
NOTIFICATION_EMAIL=info@sebastiaanpeters.nl

# Image storage: Vercel Blob (required on Vercel, optional locally)
# Get this from your Vercel dashboard → Storage → Blob
BLOB_READ_WRITE_TOKEN=
